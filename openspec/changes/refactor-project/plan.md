# Plan: refactor-project

## 里程碑
1. 结构收敛：统一实现路径，修正 CLI 调用。
2. 同步链路打通：IMAP → 解析 → DB 入库。
3. 配置与文档一致性修复。
4. 测试覆盖补齐。

## 验收标准
1. `sync run --source qqmail` 可完成 IMAP 拉取、解析、入库。
2. `transactions` 表写入成功，重复交易不重复入库。
3. README 与配置不再宣称未实现能力。
4. 测试覆盖解析器与存储层，并可通过。

## 风险与回滚
1. 风险：路径迁移导致 CLI 失效或数据入库异常。
2. 回滚：短期保留旧路径，必要时切回旧实现。
